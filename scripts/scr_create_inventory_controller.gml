///scr_create_inventory_controller()

// Check if controller isn't alredy there.
if (global.inventory_controller == noone ||
    !instance_exists(global.inventory_controller)) {
    //global.inventory_controller = instance_create(x, y, obj_inventory_controller);
}
