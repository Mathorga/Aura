///scr_create_score_controller()

// Only create controller if not alredy there.
if (global.score_controller == noone ||
    !instance_exists(global.score_controller)) {
    //global.score_controller = instance_create(id.x, id.y, obj_score_controller);
}
